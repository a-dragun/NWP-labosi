<h1>Projekti</h1>

<a href="/projects/create">Create project</a>

<ul>
  <% projects.forEach(project => { %>
    <li>
      <strong><%= project.name %></strong> – <%= project.description %>
    </li>
    <li>
      <%= project.name %> - <a href="/projects/<%= project._id %>/edit">Edit</a>
      <form action="/projects/<%= project._id %>/delete" method="POST" style="display:inline;">
          <button type="submit" onclick="return confirm('Are you sure you want to delete this project?');">Delete</button>
      </form>
    </li>

  <% }) %>
</ul>
